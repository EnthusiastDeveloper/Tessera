# Tessera

Self-hosted task scheduling that respects the real shape of your day.

Tessera fits your flexible tasks into the gaps left by fixed commitments,
deadlines, priorities, and dependencies - inside a daily capacity you set,
not just wherever a slot happens to be free.

See `docs/design-doc.md` for the product specification and
`docs/architecture-plan.md` for the implementation plan.
